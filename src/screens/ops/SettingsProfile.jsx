import PlaceholderScreen from '../../components/shared/PlaceholderScreen'

export default function SettingsProfile() {
  return <PlaceholderScreen number={24} title="Settings / Profile" phase={4} />
}
